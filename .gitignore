# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/simplevirtd
/simplevirtctl
/cmd/simplevirtd/simplevirtd
/cmd/simplevirtctl/simplevirtctl

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

/build/
/vendor/
